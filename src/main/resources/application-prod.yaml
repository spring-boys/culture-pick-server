server:
  forward-headers-strategy: framework

spring:
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: false

cookie:
  secure: true

springdoc:
  swagger-ui:
    enabled: false
  api-docs:
    enabled: false
